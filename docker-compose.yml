services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: nhutminhio:latest
    container_name: nhutminhio
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - PORT=5173
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
